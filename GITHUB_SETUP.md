# 📦 Настройка репозитория на GitHub

## Шаг 1: Создать репозиторий на GitHub

1. Откройте [GitHub.com](https://github.com) и войдите в аккаунт
2. Нажмите кнопку **"+"** в правом верхнем углу → **"New repository"**
3. Заполните форму:
   - **Repository name:** `biomech` (или другое имя)
   - **Description:** "Визуализация данных захвата движения"
   - **Visibility:** Public или Private (на ваше усмотрение)
   - **НЕ** ставьте галочки на "Initialize with README", "Add .gitignore", "Choose a license"
4. Нажмите **"Create repository"**

## Шаг 2: Подключить локальный репозиторий к GitHub

После создания репозитория GitHub покажет инструкции. Выполните команды:

```bash
# Добавить remote (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/biomech.git

# Или если используете SSH:
git remote add origin git@github.com:YOUR_USERNAME/biomech.git

# Проверить что remote добавлен
git remote -v

# Запушить код
git push -u origin main
```

## Альтернативный способ (если репозиторий уже создан)

Если вы уже создали репозиторий и пытались запушить, но получили ошибку:

```bash
# Проверить текущий remote
git remote -v

# Если remote не настроен, добавить его:
git remote add origin https://github.com/YOUR_USERNAME/biomech.git

# Если remote уже есть, но нужно изменить URL:
git remote set-url origin https://github.com/YOUR_USERNAME/biomech.git

# Запушить код
git push -u origin main
```

## Проверка

После успешного push откройте ваш репозиторий на GitHub - там должны появиться все файлы!

## Полезные команды

```bash
# Проверить статус
git status

# Посмотреть все ветки
git branch -a

# Посмотреть remote репозитории
git remote -v

# Обновить данные о remote
git fetch origin
```
