# Краткое руководство по использованию

## 📋 Доступные скрипты

### 1. `test_read_data.py` - Проверка чтения данных
**Назначение:** Быстрая проверка корректности данных без GUI.

```bash
python3 test_read_data.py
```

**Выводит:**
- Метаданные файлов
- Список маркеров
- Статистику по координатам
- Примеры данных из первого кадра

---

### 2. `plot_markers_static.py` - Статические графики
**Назначение:** Создание статических изображений траекторий и проекций.

```bash
# Просмотр графиков
python3 plot_markers_static.py milana/Measurement1.tsv

# Сохранение в файлы PNG
python3 plot_markers_static.py milana/Measurement1.tsv \
  --save-trajectories trajectories.png \
  --save-projections projections.png

# Анализ конкретного кадра
python3 plot_markers_static.py clear_data/Measurement1.tsv --frame 500
```

**Создает:**
- 3D график траекторий всех маркеров
- 2D проекции (XY, XZ, YZ) для выбранного кадра

---

### 3. `visualize_markers.py` - Анимация маркеров
**Назначение:** Интерактивная 3D анимация движения маркеров.

```bash
# Интерактивный выбор файла
python3 visualize_markers.py

# Указать конкретный файл
python3 visualize_markers.py --file milana/Measurement1.tsv

# Ускорить воспроизведение (показывать каждый 5-й кадр)
python3 visualize_markers.py --file clear_data/Measurement1.tsv --skip-frames 5

# Замедлить воспроизведение (50 мс между кадрами)
python3 visualize_markers.py --interval 50

# Сохранить в GIF
python3 visualize_markers.py --file milana/Measurement1.tsv --save animation.gif

# Сохранить в MP4 (требуется ffmpeg)
python3 visualize_markers.py --file clear_data/Measurement1.tsv --save animation.mp4
```

**Параметры:**
- `--file` - путь к TSV файлу
- `--skip-frames N` - показывать каждый N-й кадр (1 = все кадры)
- `--interval N` - интервал между кадрами в миллисекундах
- `--save FILE` - сохранить анимацию в файл (.gif или .mp4)

---

## 🚀 Быстрый старт

1. **Установить зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

2. **Проверить данные:**
   ```bash
   python3 test_read_data.py
   ```

3. **Посмотреть анимацию:**
   ```bash
   python3 visualize_markers.py
   ```

---

## 📊 Структура данных

### Папки с данными:
- `milana/` - 3 измерения Milana (Measurement1-3.tsv)
- `clear_data/` - 2 измерения Roman Eidelman (Measurement1-2.tsv)

### Формат TSV файлов:
- **Частота:** 100 Hz (100 кадров в секунду)
- **Маркеры:** 15 штук
- **Кадры:** 2000 (20 секунд записи)
- **Координаты:** X, Y, Z в миллиметрах

---

## 💡 Советы

### Для медленных компьютеров:
```bash
# Показывать каждый 10-й кадр
python3 visualize_markers.py --skip-frames 10
```

### Для детального анализа:
```bash
# Замедленное воспроизведение
python3 visualize_markers.py --interval 100
```

### Для создания презентаций:
```bash
# Статические графики в высоком качестве
python3 plot_markers_static.py milana/Measurement1.tsv \
  --save-trajectories trajectories.png \
  --save-projections projections.png
```

---

## ⚠️ Возможные проблемы

### Ошибка: "Command 'python' not found"
**Решение:** Используйте `python3` вместо `python`

### Ошибка при сохранении в MP4
**Решение:** Установите ffmpeg:
```bash
sudo apt-get install ffmpeg
```

### Медленная анимация
**Решение:** Используйте параметр `--skip-frames`:
```bash
python3 visualize_markers.py --skip-frames 5
```
