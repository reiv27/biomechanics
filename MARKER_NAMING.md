# 🏷️ Система именования маркеров

## Автоматическое присвоение имен

Скрипты автоматически присваивают простые имена маркерам на основе их позиций в пространстве.

### Как это работает:

1. **Анализ позиций**: Вычисляется средняя позиция каждого маркера по всем кадрам
2. **Определение центра**: Вычисляется центральная точка по Y-координате (лево-право)
3. **Временное разделение**: Маркеры временно делятся на левые/правые для определения позиций
4. **Фильтрация**: Исключаются ненужные маркеры (l1, l6, l5, r5, r8, r2)
5. **Нумерация**: Оставшиеся маркеры нумеруются просто как **1, 2, 3, 4...**

### Формат имен:

- **`1, 2, 3, 4, 5...`** - все маркеры по порядку (после фильтрации)

### Исключенные маркеры:

Следующие маркеры автоматически исключаются из визуализации:
- **l1, l5, l6** - левые маркеры
- **r2, r5, r8** - правые маркеры

### Пример:

```
Оригинальное имя → Временное имя → Итоговое имя
New 0000         → r8           → (исключен)
New 0001         → l1           → (исключен)
New 0002         → l3           → 1
New 0003         → l4           → 2
New 0004         → r1           → 3
New 0005         → r6           → 4
```

## Где используются простые имена:

### 1. В анимации (`visualize_markers.py`)
- Подписи рядом с каждой точкой
- Большие, жирные, синие буквы
- Обновляются в каждом кадре

### 2. На статических графиках (`plot_markers_static.py`)
- **3D траектории**: подписи у начальной позиции
- **2D проекции**: подписи рядом с каждой точкой
- Черные, жирные надписи для четкости

### 3. В выводе данных (`test_read_data.py`)
- Показывает соответствие: `r8 (New 0000)`
- Упрощает идентификацию маркеров

## Преимущества:

✅ **Краткость**: `r1` вместо `New 0004`
✅ **Понятность**: сразу видно где маркер (слева/справа)
✅ **Порядок**: нумерация отражает пространственное расположение
✅ **Удобство**: легко ссылаться на маркеры в обсуждениях

## Кастомизация:

Если вам нужна другая логика именования, можно изменить метод `_create_simple_names()` в файле `visualize_markers.py`.

Например:
- Использовать другую координату (X или Z)
- Другие префиксы (вместо l/r)
- Другой порядок сортировки
