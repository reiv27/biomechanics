# 🎉 Что нового: Фильтрация и простая нумерация маркеров!

## Главные улучшения

1. **Автоматическая фильтрация**: Ненужные маркеры (l1, l5, l6, r2, r5, r8) исключаются автоматически
2. **Простая нумерация**: Оставшиеся маркеры нумеруются как **1, 2, 3, 4...**
3. **Подписи на всех визуализациях**: Каждый маркер подписан своим номером

## До и После

### Раньше:
```
15 маркеров: New 0000, New 0001, New 0002...
```
❌ Много лишних маркеров  
❌ Непонятные имена  
❌ Без подписей на графиках  

### Теперь:
```
9 маркеров: 1, 2, 3, 4, 5, 6, 7, 8, 9
```
✅ Автоматически отфильтрованы ненужные  
✅ Простые номера  
✅ Подписи на всех визуализациях  

## Как это выглядит

### 1. В тестовом выводе
```bash
python3 test_read_data.py data/Measurement1.tsv
```

**Результат:**
```
🎯 Маркеры (9 шт.):
  Простые имена и оригинальные:
  1 (New 0002)
  2 (New 0003)
  3 (New 0004)
  4 (New 0005)
  5 (New 0006)
  ...
```

**Исключено 6 маркеров:** l1, l5, l6, r2, r5, r8

### 2. На 3D анимации
```bash
python3 visualize_markers.py data/Measurement1.tsv
```

**Вы увидите:**
- 9 красных точек с черными контурами
- Синие жирные подписи: **1**, **2**, **3**, **4**...
- Подписи двигаются вместе с точками
- Легко отследить каждый маркер

### 3. На статических графиках
```bash
python3 plot_markers_static.py data/Measurement1.tsv
```

**Результат:**
- **3D траектории**: каждая линия подписана у начальной точки
- **2D проекции**: подписи рядом с каждой точкой
- Большие точки с контурами для лучшей видимости

## Логика фильтрации и нумерации

### Автоматический процесс:
1. Вычисляется средняя позиция каждого маркера
2. Определяется центр по Y-координате (лево/право)
3. Маркеры временно делятся на левые и правые
4. **Исключаются ненужные**: l1, l5, l6, r2, r5, r8
5. Оставшиеся 9 маркеров нумеруются: **1, 2, 3, 4, 5, 6, 7, 8, 9**

### Результат:
- Было 15 маркеров → Осталось 9
- Простая нумерация вместо сложных имен

## Примеры использования

### Быстрая проверка маркеров
```bash
# Посмотреть какие маркеры где расположены
python3 test_read_data.py data/Measurement1.tsv
```

### Анимация с подписями
```bash
# Интерактивная 3D анимация с подписями
python3 visualize_markers.py data/Measurement1.tsv
```

### Создать графики для презентации
```bash
# Статические графики с подписями
python3 plot_markers_static.py data/Measurement1.tsv \
  --save-trajectories trajectories.png \
  --save-projections projections.png
```

### Замедленная анимация для анализа
```bash
# Медленная анимация, чтобы рассмотреть каждый маркер
python3 visualize_markers.py data/Measurement1.tsv --interval 100
```

## Преимущества

✅ **Автоматическая фильтрация** - ненужные маркеры исключены  
✅ **Простая нумерация** - просто 1, 2, 3...  
✅ **Наглядно** - подписи на всех графиках  
✅ **Меньше данных** - 9 вместо 15, быстрее обработка  
✅ **Удобно** - легко ссылаться: "посмотри на маркер 3"  

## Дополнительная информация

- **MARKER_NAMING.md** - подробное описание системы именования
- **CHANGELOG.md** - полный список изменений
- **README.md** - обновленная документация

---

**Попробуйте прямо сейчас:**
```bash
python3 visualize_markers.py data/Measurement1.tsv
```

Посмотрите, как легко теперь отслеживать каждый маркер! 🎯
